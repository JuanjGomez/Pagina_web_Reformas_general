[build]
  # Directorio a publicar (raíz del proyecto)
  publish = "."
  
  # No hay comando de build porque es HTML estático
  command = ""

# Configuración de headers para seguridad y rendimiento
[[headers]]
  for = "/*"
  [headers.values]
    # Seguridad
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    
    # Permisos
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Cache para assets estáticos
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache para imágenes
[[headers]]
  for = "*.jpg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.webp"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Redirects para URLs limpias (opcional)
# [[redirects]]
#   from = "/servicios"
#   to = "/pages/servicios.html"
#   status = 200

# [[redirects]]
#   from = "/contacto"
#   to = "/pages/contacto.html"
#   status = 200